version: '3.8'

services:
  app:
    build: .
    ports:
      - "8083:80"
    volumes:
      - ./database:/var/www/html/database
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_URL=http://localhost:8083
    restart: unless-stopped
